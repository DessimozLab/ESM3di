ESM3di/                               # Repository Root
├── src/                              # Clean Python source directory
│   └── esm3di/                       # Importable package
│       ├── __init__.py               # Exposes ESM3DiPredictor
│       ├── cli.py                    # Main CLI (`esm3di predict`, `esm3di foldtree`)
│       ├── model.py
│       ├── inference.py
│       ├── io.py
│       ├── preprocessing.py
│       │
│       └── workflows/                # EMBEDDED Viral-FoldTree Engine
│           ├── __init__.py
│           ├── Snakefile             # Clean Snakemake workflow entry point
│           ├── config.yaml           # Default runtime parameters
│           ├── rules/                # Modular Snakemake rules (.smk)
│           ├── envs/                 # Conda YAML environment definitions
│           └── scripts/              # Helper scripts called directly by Snakefile
│
├── tests/                            # Fast unit/integration tests for pytest
│   ├── test_model.py
│   └── test_workflow.py              # Runs `snakemake --dry-run` against test dataset
│
├── test_data/                        # Minimal test inputs (tracked in Git)
│   └── test_virus.fasta              # Small test sequence file for quick verification
│
├── checkpoints/                      # Heavy Model Weights
│   └── hf_compatible/                # Tracked via Git LFS (Excluded from pip wheel)
│
├── paper_results/                    # Paper Reproducibility & Benchmarks
│   ├── data/
│   └── run_paper_figures.sh          # Recreates manuscript figures
│
├── research_archive/                 # Dave's Original Work
					LEAVING IN ITS ORIGINAL (current master branch) 

│   └── legacy_readme.md              # Colleague's guide to navigating old experiments
│
├── MANIFEST.in                       # Guarantees what gets packed into sdist/wheel
├── .gitignore                        # Prevents committing run outputs
├── README.md                         # Primary user documentation
└── pyproject.toml                    # Modern packaging configuration