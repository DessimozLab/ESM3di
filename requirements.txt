# Core dependencies
torch>=2.0.0
transformers>=4.46.0,<4.48.2
peft>=0.10.0,<0.12.0
accelerate>=0.20.0

# ESM tokenizer dependencies
sentencepiece
protobuf

# Optional: ESMC model support (EvolutionaryScale models)
# esm  # Uncomment to enable ESMC-300M and ESMC-600M models

# Optional: for better performance
bitsandbytes  # For 8-bit optimization
pebble
pandas
tensorboard
